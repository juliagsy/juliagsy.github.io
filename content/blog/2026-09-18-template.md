---
title: Post template
date: 2026-09-18
tags: [notes]
summary: Every piece of markdown formatting, in one place. Kept as a draft, so it stays in the repo and off the site.
draft: true
---

A reference for what the formatting looks like, not a real post. `draft: true` in the
frontmatter above keeps it out of the blog list and stops it getting a page of its
own — copy this file to start a post, and drop the `draft` line when it is ready.

## Writing a post

Add a file to `content/blog` named `<YYYY-MM-DD>-<slug>.md`. The date prefix keeps
the directory in chronological order and the slug becomes the URL, so a post in
`2026-09-18-hello.md` lives at `/others/blog/hello`.

The frontmatter needs `title`, `date`, `tags` and `summary`. Tags have to exist in
`components/tags.json` — the build fails on one that does not, rather than quietly
dropping the post out of a filter.

## Formatting

Ordinary prose, **bold**, *italic*, `inline code`, and [links](https://arxiv.org)
all work. So do lists:

- unordered items
- with a second line
- and a third

1. ordered items
2. also work

> A blockquote, for when someone else said it better.

Code blocks keep their whitespace:

```python
def papers(topic: str) -> list[str]:
    return [p for p in fetch(topic) if p.is_interesting]
```

| Table | Support |
| --- | --- |
| GitHub-flavoured | yes |
| Alignment | yes |

---

That is the whole vocabulary. Anything markdown supports, `marked` renders.
